<!--pages/recite.wxml-->
<popup id='popup' 
    top="{{popwindow.top}}"
    k="{{popwindow.k}}"
    v="{{popwindow.v}}"
    btn_inc="+1"
    btn_sub="-1"
    bind:popup_inc="popwindow_inc"  
    bind:popup_sub="popwindow_sub">
</popup>


<view class="table">
  <view class="tr_first bg-w">
    <view class="th"><switch checked="" bindchange="switch1Change"/></view>
    <view class="th"><switch checked="{{switch1Checked}}" bindchange="switch2Change"/></view>
    <view class="th"><switch checked="" bindchange="switch3Change"/></view>
  </view>
  <block wx:for="{{words}}" wx:key="k">
    <view class="tr" data-item="{{item}}" data-item="{{item}}" bindtap="wordClick">
      <row k="{{item.k}}" v="{{item.v}}" count="{{item.count == 0?'':item.count}}" hide_v="{{row_hide_v}}"></row>
    </view>
  </block>
  <block wx:for="{{words}}" wx:key="k">
    <view class="tr" data-item="{{item}}" data-item="{{item}}" bindtap="wordClick">
      <row k="{{item.k}}" v="{{item.v}}" count="{{item.count == 0?'':item.count}}" hide_v="{{row_hide_v}}"></row>
    </view>
  </block>
</view>



